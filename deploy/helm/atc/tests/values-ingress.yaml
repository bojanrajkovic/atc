ingress:
  enabled: true
  className: nginx
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  hosts:
    - host: atc.example.com
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: atc-tls
      hosts:
        - atc.example.com
